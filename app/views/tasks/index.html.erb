<h1>タスクの一覧ぺーじだお(^ω^)</h1>

<ul>
  <% @tasks.each do |task| %>

    <li>
      <%= link_to task.id, task %>
      :
      <%= task.title %>
        &gt;
      <%= task.content %>
        &gt;
      <%= task.status %>
    </li>

    <% end %>
</ul>

<%= link_to '新しいタスクを追加する', new_task_path %>
